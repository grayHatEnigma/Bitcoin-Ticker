name: bitcoin_ticker
description: a crypto currency price checking app.


version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2
  http: ^0.12.0+2
  flutter_spinkit: "^4.1.0"

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:


  uses-material-design: true
